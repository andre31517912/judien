// Re-exports the event detail screen so it can be pushed within the groups stack.
// This ensures the back button returns to the group detail rather than switching to the events tab.
export { default } from '../../../events/[id]';
